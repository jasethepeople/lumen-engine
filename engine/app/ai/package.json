{
  "name": "@lumen/app-ai",
  "version": "0.1.0",
  "description": "AI authoring assistant for Lumen: local, zero-network scene generation from text descriptions, motion/chapter/camera suggestions, asset tagging and colorway detection, and template recommendations. Default provider is a fully local heuristic engine; MockAIProvider offers deterministic scripted responses for tests.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build && node --test test/"
  },
  "license": "UNLICENSED"
}
